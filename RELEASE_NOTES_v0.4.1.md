# Falcon Render v0.4.1

Blender 5.2.1 LTS ベース。**この版の主題は DLSS Ray Reconstruction(DLSS-RR)によるレンダリングです。**
v0.4 は動画編集(VSE)でした。v0.4.1 は、Cycles のデノイザとして DLSS-RR を
**アニメーション(連番)で実用の形にした**版です。あわせて煙・VDB のコマが軽くなりました。

起動時バージョン表示: `Falcon Render v0.4.1`

> DLSS のバイナリ(`libnvidia-ngx-dlssd.so`)は v0.4 と同じく**同梱していません**。
> 受け取り口だけが入っているので、NVIDIA から正規に入手した .so を展開先の直下に置くと有効になります
> (README「DLSSについて」)。NVIDIA への事前通知は提出済み、商標表示は承認待ちです。

---

## ★ DLSS-RR がアニメーションで使える形になりました

### 1. カメラ切り替え(カット)の直後がざらつかない

連番レンダーで**カメラを切り替えたコマの直後だけ**粒が残っていました(tree の 812 / 966 / 603 コマ目など)。
原因は「温めが足りない」ではなく、**カメラ切り替えが最終レンダーでは一度も検出されていなかった**ことです
(ビューポートでは動いていた検出が、連番レンダーではセッションがコマごとに作り直されるため常に偽だった)。

700〜816 コマの1ジョブ・812 コマ目・高周波残差 RMS(小さいほど滑らか)・2026-09-04 実測:

| | 残差 RMS | 明るさ |
|---|---|---|
| v0.4(検出されない) | 0.04583(隣のコマの 1.88 倍) | — |
| 検出だけ切る | 0.01960 | 0.2455(**前のショットの残像**・本来 0.169) |
| **v0.4.1(検出する)** | **0.02413(隣の 0.96 倍)** | **0.1621** |

- 費用は 0 秒です(再レンダーせず、履歴を捨てるだけ)。
- ★**粒の数字だけを見ると「残像」のほうが良く見えます**(滑らかなので)。明るさの列を必ず一緒に見てください。
- 「同じ入力で DLSS を N 回まわす」は採りませんでした: 粒は直りますが**明るさが回数に応じて落ちます**(N=4 で 0.083 / N=8 で 0.080)。
- UI: レンダープロパティ > デノイズ「**カメラ切り替え時の事前レンダリング**」(既定 ON)。
  カメラが2台以上・カメラ束縛マーカーが1つ以上あるシーンでだけ出ます。
- 戻す口: そのスイッチ、または `FALCON_DLSS_CUT_WARMUP=0`

### 2. 連番の費用は「初回だけ」

1391 コマ(tree・FHD・DLSS-RR)を一晩で通した実測(2026-09-04):

| | |
|---|---|
| 全コマ | **83.6 分**(1コマ中央値 **3.51 秒**・抜け 0) |
| 1コマ目 | 23〜34 秒(NGX の初期化が +12〜25 秒) |
| 2コマ目以降 | 2〜3 秒 |

**1コマだけ焼いて DLSS の費用を語ることはできません。**初期化はジョブに1回です。

### 3. OIDN と、どちらを使うか(正直な数字)

同じ時間で比べた**1枚の正確さ**は、この版でも **OIDN が全帯域で DLSS-RR に勝ちます**
(tree 0 コマ目 0.01212 対 0.01492 / 700 コマ目 0.03685 対 0.05058・256spp 参照との誤差・小さいほど良い)。
DLSS-RR を選ぶ理由は次の2つです:

- **4K ビューポートの速さ**(1spp を流したときに下がり続けるのは DLSS だけ)
- **連番の安定**(上の 1・2)

静止画1枚なら OIDN、動くものと 4K の作業なら DLSS-RR、が今の推奨です。

---

## 煙・VDB のコマが軽くなりました

煙(流体ドメイン・VDB)のあるコマで、**箱の 6 面しか作らないのに毎コマ NanoVDB → OpenVDB のマスクへ戻して**境界を求めていた段を消しました。

| 解像度 | v0.4 | v0.4.1 | |
|---|---|---|---|
| res160・1コマ | 2.335 秒 | **1.912 秒** | −18.1% |
| 同・常駐データあり | 1.555 秒 | **1.105 秒** | −28.9% |

- 費用の本体は「DLSS が動きベクトルを要求して速度グリッドが載るとき」の膨張でした。既定が DLSS なので必ず踏む所です。
- 戻す口: `FALCON_VOLUME_BBOX_FAST=0`
- 「常駐データ(Persistent Data)」は既定 ON になりました(本人の startup.blend が OFF を持っていたため 5 週間効いていませんでした)。

---

## 動画編集(VSE)の続き

- **書き出しで何が効いたかを1行で出します**: 出力プロパティに
  `fast path: 使った (1 cuts, 3 frames) / 符号化: そのままコピー / 0.3 s` のような行が出ます。
  fast path と GPU 符号化は別々の層が決めていて、失敗すると黙って CPU に戻るため、決まった後の実物の名前を残す形にしました。
- 画面に既に Sequencer がある時は「VSE で編集」のボタンを出しません(共有チェックと出力名の欄は残ります)。
- ⚠ NVENC には最小サイズがあり、64x36 のような極小では黙って CPU に戻ります(640x360 なら NVENC)。

---

## その他

- CyclesF のパネルに「Caustica の形」を用意(既定 OFF)。
- CyclesF がレンダープロパティから消えていたのを直した。
- 互換性の門 `flcompat`(素の Blender と同じ絵 / 往復で壊れない / 主要アドオンと同居)を木に入れた。

## 起動と UI(Linux)

- **Hyprland / Sway / river では XWayland で起動します**(`falcon-render.sh`)。Wayland 直だと `SUPER+←` 等でフォーカスを渡した時に
  OS キーが押しっぱなしに閂されてクリックが効かなくなり(Blender #161108)、ファイルブラウザが 320x240 で出ます(#162315)。
  上流が案内している回避策をそのまま既定にしました。戻す口: `FALCON_FORCE_X11=0`(常に Wayland)/ `=1`(他のデスクトップでも XWayland)。
- **初回起動だけ**、プリファレンスの2点を既定にして保存します(`userpref.blend` が無い時のみ・2回目以降は触りません):
  インターフェース > 表示 > **解像度スケール 1.25**(約 110 PPI のモニタ向け・工場 1.0 は約 96 PPI 向け)/
  一時的なエディター > **ファイルブラウザ = 最大化エリア**(新しい OS 窓を作らないので 320x240 の問題そのものが消える)。
  どちらもプリファレンスで自由に変えられます。`--factory-startup` と `FALCON_NO_FIRST_RUN=1` では何もしません。

## 既知の問題

`KNOWN_ISSUES.md` を参照。
