# 既知の不具合

*[English version / 英語版はこちら](KNOWN_ISSUES.en.md)*

現在調査中、または未解決の不具合をここにまとめます。
再現条件・切り分け状況は分かっている範囲で更新していきます。

## 【v0.2】GPU レンダーが使えなかった — v0.3 で修正済み

- **症状**: 配布した v0.2 で GPU レンダーが丸ごと失敗する。CPU レンダーは通る
- **原因**: GPU のカーネルは**実行時に**同梱ソースを読んでコンパイルする。その一覧
  (`SRC_KERNEL_*_HEADERS`)にフォーク固有の `falcon_sharc_size.h` と
  `falcon_lighttrace.h` が載っておらず、同梱されていなかった。
  **ビルドも CPU レンダーも通るため、ビルドでは絶対に見つからない型の不具合**
- **修正**: 一覧に追加。加えて、配布したソースだけでカーネルが閉じているかを確かめる
  検査 (`tools/falcon_check_kernel_install.py`) をパッケージ作成の手順に入れ、
  **通らなければパッケージを作らない**ようにした

## 【v0.2】他人の `.blend` を開いて踏める3件 — v0.3 で修正済み

`.blend` に書かれた本ビルド固有の設定値が、読み込み時に検証されずそのまま使われていた。
Blender の PropertyGroup は IDProperty なので、RNA の上限が当て直されない。

| | v0.2 | v0.3 |
|---|---|---|
| 任意のファイルが 1 GiB で上書きされる | 踏める | 塞いだ(`~/.cache/falcon_photon/` へ読み替え) |
| 確保より大きく読む | 踏める | 撥ねて完走 |
| 異常な半径で戻ってこなくなる | 素通り | 8.0 に切り詰め |

正常系は変えていない(同じシーンのコースティクス出力がバイト単位で一致・速度 0.963〜1.021 倍)。

## 【v0.2】コンポジターを組んだシーンでライトトレースが画面全体を明るくする — v0.3 で修正済み

- **症状**: コンポジターノードを組んだシーンで、ライトトレースの層が集光と無関係な画素まで
  一様に明るくする。公式デモの「バルセロナ・パビリオン」で参照の 10.9 倍
- **原因**: 層をファイル経由で読み戻していて、読んでいたのが Cycles の生の出力ではなく
  **シーンのコンポジターを通した後の絵**だった。ビューティも同じ経路なので、コンポジターが
  画面全体へ足す一定量を**二重に数えていた**。
  社内の試験シーンはコンポジターノードが0個だったため約1か月見つからなかった
- **修正後**(同じシーン): 空 1.212→**1.0000** / 壁 1.395→1.0002 / 屋根 1.277→**1.0000** /
  水面の集光 3.160→**1.0162**。層のエネルギーは 197 分の1、被覆は 99.999%→0.0998%

## 【v0.2】光源に大きさを与えるとライトトレースが画面全体を明るくする — v0.3 で修正済み

- **症状**: スポットライトに半径を与えると、集光と無関係な画素まで一律に明るくなる
  (全画素に +0.078 の下駄。いちばん暗い背景では参照の 5.8 倍に見える)
- **原因**: 光子がランプそのものに当たったとき、その放射をフィルムへ書いていた。
  光子パスの画素番号は「光子の発射番号」なので、書くとフレーム全体へばらまかれる
- **修正後**: 全画素の下駄 +0.078 → **−0.0009**。対照領域は参照比 0.996〜1.000

## 【v0.4】動画編集(VSE)まわり

v0.4 で入った VSE の高速化について、**分かっている上で残している性質**です。

- **fast path でビット一致するのは「コピー区間」だけ**です。つなぎ目は焼き直されるので、
  そこだけ元素材と画質が異なります(crf 16 相当)。コピー区間は素材とビット単位で一致します
  (240 コマ中 240 コマで確認)
- **fast path の出力は通常の書き出しより僅かに容量が大きくなります。**
  コピー区間が素材のビットレートそのままだからです
- **fast path は条件が合わないと黙って通常の書き出しへ降ります。**降りる条件は
  出力 codec が素材と違う / 可変フレームレート / インターレース / 回転メタデータ付き /
  HDR / エフェクトやモディファイアが付いている。**降りたこと自体は失敗ではありません**
- **DNxHD での書き出しが失敗することがあります。**これは **素の Blender 5.2 でも同じ**で、
  上流側の既存の不具合です(GPU エンコードの有無に関係ありません)
- **レンダー領域でカメラカリングを絞ると、領域の外にある光源はその照明ごと消えます。**
  既定の余白(margin = 1.0)では何も変わらないので、既存のファイルは黙って変わりません。
  パネル(簡略化 > カリング)から切れます

## 【v0.4】DLSS-RR: カメラが切り替わった直後の1コマがざらつく(2026-09-04・調査済・修正中)

- **症状**: 連番レンダーで、カメラに結んだマーカーで視点が切り替わるコマ(例: 603 / 812 / 966)だけ粒状の雑音が残る。次のコマから1〜2コマで戻る。開始コマも弱く出る
- **原因**: DLSS-RR の時間履歴が切り替わりで無効になり、そのコマは履歴1コマ分の蓄積しか無い。同じコマは NGX の feature を作り直すので時間も掛かる(中央値 3.5 秒に対し 20〜37 秒)
- **VSE や NVENC の書き出しは無関係**(出力 PNG に既にある)
- **修正の形**: 切り替わりのコマだけ、同じ入力で DLSS を数回まわして履歴を温める(再レンダー不要)。カメラが2台以上でマーカーがある時に「カメラ切り替え時の事前レンダリング」の項目を出す予定
- **参考の数字(FHD 32spp・一晩 1391 コマ)**: DLSS の追加費用は**ほぼ初回の初期化だけ**(1コマ目 +12〜25 秒・2コマ目以降 2〜3 秒)。静止画1枚で測ると DLSS が遅く見えるが、連番では OIDN と同じ帯。画質は同じ台で OIDN が全帯域で上(RMSE 0.0121 対 0.0149)なので、**最終出力は OIDN、時間が惜しい時は DLSS** が今の使い分け

## 未解決

- **受光面に食い込んだガラス**で、ガラス本体が過大に明るくなります(総当たりの真値の **20.7 倍**)。
  **原因は幾何です**: 受け側の面がガラスの壁を貫いていると、壁を導かれた光がガラスの**内側にある**
  受光面へ落ちます。そこは接触円 = 線なので、固定セルの密度推定が発散します。

  | ガラスと机の関係 | ガラス本体の明るさ(真値比) |
  |---|---|
  | 1.1 mm 食い込み | **20.7 倍** |
  | 0.6 mm 食い込み | 20.1 倍 |
  | **ちょうど接触(隙間 0)** | **1.90 倍** |
  | 5 mm 浮かせる | 1.68 倍 |
  | 20 mm 浮かせる | 1.29 倍 |

  **回避: 受光面に食い込ませないでください**(接触は可・交差は不可)。深さは関係なく、
  食い込んでいるかどうかだけで決まります。0.6 mm でも起きます
- **接地した集光の「芯」は、どの近似でも出せていません。** 接触円の光を真値の上位 1% で見ると、
  蓄積グリッド 0.075 / ライトトレース 0.031 / 公式 MNEE 0.053(総当たりのパストレースだけが 0.964)。
  ちょうど接触させた場合も、ガラス本体は 1.90 倍のまま残ります(セル寸法を倍にすると超過が半分に
  なる = 密度推定の原理的な限界)
- **屋内や水中など、光が何度も跳ね返る場面**ではライトトレースの層が暗い側に出る
  (水中で参照の 0.556 倍)
- ライトトレースの焼きは、短い予算では固定費が大半を占める(60 秒の予算のうち 57 秒が焼き)

## 【v0.1】DLSS 連番レンダーでサンプル数が指定値の何倍も消費される (#2, #3) — v0.2 で修正済み

- **症状**: DLSS-RR有効時、連番(アニメーション)レンダーで指定サンプル数を大幅に超過して
  消費し続ける。静止画では発生しない
- **原因**: `intern/cycles/blender/session.cpp`。Persistent Data(既定OFF)がOFFの場合、
  アニメーションの毎フレームで`BlenderSession`(と内部の`Session`/`RenderScheduler`)が
  丸ごと破棄・再構築される。DLSS-RRの「最初のフレームだけ5倍サンプルで時間履歴を温める」
  仕組み(`get_dlss_preroll_passes()`)の初回判定フラグ`dlss_history_cold_`が、この
  再構築のたびに初期値`true`へ戻るため、**全フレームが「初回」扱いされ毎回5倍消費**する
- **修正**: `BlenderSession::render()`完了時に「このジョブでDLSS-RR履歴は温まった」と
  記録し、次にSessionが再構築された際その記録を渡して`dlss_history_cold_`を`false`に
  戻す。記録場所は`BlenderSession`のインスタンスメンバではなく`static`メンバ
  (`dlss_history_warmed_this_job`)にする必要がある。`BlenderSession`自体が毎フレーム
  作り直されるため、インスタンスメンバでは値が生き残らない(最初にこれで一度失敗した)
- **修正の副作用と再修正**: `static`にした直後、今度は逆方向のバグが出た
  (プロセス全体で1度でも温まると、以降**新しいジョブの本当の1枚目までx1**扱いになり、
  蓄積不足でノイズが増える)。`cfra == sfra`(現在フレームが範囲の開始フレームと一致)を
  「新しいジョブの1枚目」の判定に使い、その時だけ強制的にコールドへ戻す形で解消。
  2つの連続ジョブ(各2フレーム)で検証し、両方とも1枚目コールド・2枚目warmを確認済み
- **実機検証(classroomシーン、2フレーム、Persistent Data OFF)**:
  修正前 20.8〜24.4秒 → 修正後 14.5秒(8spp)。デバッグログでフレーム2の
  `warmed_flag`が`0`→`1`に変わったことも確認済み
- **対象コミット**: `intern/cycles/integrator/render_scheduler.{h,cpp}`,
  `intern/cycles/session/session.{h,cpp}`, `intern/cycles/blender/session.{h,cpp}`
  (Blender-5.2.C2-dlss、ローカルソース側)

## メモ

- ~~`FALCON_DLSS_PREROLL` はUI未露出(環境変数のみ)~~ → **v0.2で解消**。
  デノイズパネルの「履歴持ち越し」の下に**「初回蓄積レンダリング回数」**(既定4・0で無効)を
  追加した。環境変数も従来どおり効き、指定した場合はUIより優先する(A/B計測用)。
  なおこの設定は**アニメーションレンダーの1枚目にだけ**掛かる。静止画1枚のレンダーでは
  意図的に発火しない(静止画は指定サンプル数を守る、という設計)
- ビューポート(RENDERED表示)側で`preview_denoising_carry_history`(既定OFF)が
  `get_num_samples()`を`MAX_SAMPLES`に差し替える件は、今回の連番問題とは別経路。
  未対応
- デバッグ用ログ(`FALCON_DEBUG_LIFECYCLE`環境変数で有効化、BlenderSession/Sessionの
  生成・破棄・warmed_flagの値を出力)はソースに残置。次回同系統の調査で再利用可能

## 注記

このリポジトリはビルド配布・ドキュメント用であり、実際にレンダリングを行う
Cycles / DLSS-RR 統合部分のソースコードは含まれていません(詳細は [SOURCE.md](SOURCE.md) を
参照)。そのため、本問題の原因調査・実装修正は、`falcon-render-v0.4-src.tar.xz` を展開した
別リポジトリ(ローカル)側で行う必要があります。このリポジトリ側では、症状の記録と
切り分け状況の追跡のみを行います。
